name: Publish Website to Web Hosting V2
on:
  push:
    branches:
      - main
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v2

      - name: 📂 Sync files
        with:
          server: ftp.bccbank.in
          username: bccbank321@bccbank.in
          password: 4([]@HhY}P{I
          port: 21
